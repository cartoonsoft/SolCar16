@model AdmCartorio.Models.ArquivoModeloDocxViewModel

@{
    ViewBag.Title = "Editar";
}
<script src="~/Scripts/libs/jquery-2.1.1.min.js"></script>

<article class="col-sm-12 col-md-12 col-lg-6 sortable-grid ui-sortable" id="content">
    <div class="jarviswidget jarviswidget-sortable" id="wid-id-1" data-widget-custombutton="false" data-widget-editbutton="false" data-widget-colorbutton="false">
        <div>
            <div class="widget-body no-padding">
                @using (Html.BeginForm("Editar", "Arquivos", FormMethod.Post, new { enctype = "multipart/form-data", id = "formEnvio", @class = "smart-form" }))
                {
                    @Html.AntiForgeryToken()

                    <header>
                        <h1>Edição de Modelos</h1>
                    </header>
                    <fieldset>
                        <!-- SELECAO DE ARQUIVO-->
                        <div class="row">
                            <section class="col col-6">
                                <label class="col-form-label">Selecione um modelo</label>
                                @Html.TextBoxFor(model => model.Files, null, new { @type = "file", @accept = "docx", @name = "file", @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.Files, null, new { @class = "text-danger" })

                                @Html.HiddenFor(model => model.LogArquivoModeloDocxViewModel.IP)
                                @Html.HiddenFor(model => model.Id)
                            </section>
                        </div>
                        <!-- SELECAO DE ARQUIVO-->

                    </fieldset>
                    <footer>
                        <button type="submit" id="upload" class="btn btn-primary">
                            <i class="glyphicon glyphicon-upload"></i>
                            Atualizar
                        </button>
                        <button class="btn btn-default col-sm-2" onclick="window.history.back();" type="button">
                            Voltar
                        </button>
                    </footer>
                }
            </div>
        </div>
    </div>
    <iframe src="~/App_Data/testeWord.docx" hidden></iframe>

</article>

@*<iframe src="~/App_Data/Modelos/@(Model.NomeArquivo+Model.ExtensaoArquivo)" hidden></iframe>*@

<script type="text/javascript">


    debugger;

     @*$('#upload').on('click',
        function () {
            var MeuIp = document.getElementById('ipDaMaquina').value;
            var form = $('#formEnvio');
            var token = $('input[name="__RequestVerificationToken"]', form).val();


            $.ajax({
                url: '@Url.Action("UploadArquivos", "Arquivos")',
                type: 'POST',
                dataType: 'json',
                data: {
                    ip : MeuIp,
                    __RequestVerificationToken: token,
                },
                success: function (result) {
                    console.log(result);
                    console.log("Deu certo");
                },
                error: function (error) {
                    console.log(error);
                    console.log("Deu erro");
                }
            })
        });*@
    function CadastrarLogDownload(){

        var IP = document.getElementById('LogArquivoModeloDocxViewModel_IP').value;
        var form = $('#formEnvio');
        var token = $('input[name="__RequestVerificationToken"]', form).val();

        $.ajax({
            url: '@Url.Action("CadastrarLogDownload", "Arquivos")',
            type: 'POST',
            dataType: 'json',
            data: {
                IP: IP,
                __RequestVerificationToken: token,
            },
            success: function (result) {
                console.log(result);
                console.log("Deu certo");
            },
            error: function (error) {
                console.log(error);
                console.log("Deu erro");
            }
        });
    }


    function getIp(callback) {
        function response(s) {
            callback(window.userip);

            s.onload = s.onerror = null;
            document.body.removeChild(s);
        }

        function trigger() {
            window.userip = false;

            var s = document.createElement("script");
            s.async = true;
            s.onload = function () {
                response(s);

            };
            s.onerror = function () {
                response(s);
            };

            s.src = "https://l2.io/ip.js?var=userip";
            document.body.appendChild(s);
        }

        if (/^(interactive|complete)$/i.test(document.readyState)) {
            trigger();
        } else {
            document.addEventListener('DOMContentLoaded', trigger);
        }
    }

    getIp(function (ip) {
        console.log(ip);
        // Adiciona Campo Hidden para pegar o IP
        //document.getElementById("ip").value = ip;
        document.getElementById("LogArquivoModeloDocxViewModel_IP").value = ip;
        CadastrarLogDownload();
    });
</script>